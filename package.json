{
  "name": "wildcare",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/backend\" \"npm run dev --workspace=apps/dashboard\"",
    "dev:backend": "npm run dev --workspace=apps/backend",
    "dev:dashboard": "npm run dev --workspace=apps/dashboard",
    "dev:frontend": "npm run dev --workspace=apps/dashboard",
    "build": "npm run build --workspace=apps/dashboard",
    "install:all": "npm install",
    "lint": "npm run lint --workspaces",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/config-conventional": "^21.2.0",
    "@types/node": "^26.1.2",
    "commitlint": "^21.2.1",
    "concurrently": "^10.0.4",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@12.6.0",
  "lint-staged": {
    "apps/backend/**/*.{js,ts}": [
      "eslint --config apps/backend/eslint.config.js --fix",
      "prettier --write"
    ],
    "apps/frontend/**/*.{js,jsx,ts,tsx}": [
      "eslint --config apps/frontend/eslint.config.mjs --fix",
      "prettier --write"
    ],
    "*.{json,md,css,scss}": [
      "prettier --write"
    ]
  }
}
